{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security-rules unit tests (arm_counter). Run inside the Firestore emulator.",
  "scripts": {
    "test": "mocha arm_counter.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "mocha": "^10.4.0"
  }
}
